# ClassicRunDex

Thai-first Player Guide for Cookie Run Classic progression decisions.

## Run

```bash
npm install
npm run dev
```

## Verify

```bash
npm run typecheck
npm test
npm run build
```

The first durable app path is intentionally small: open the app, see the Thai Player Guide answer first, then inspect nearby Player Data and Source Records. Historical Cookie Run Kakao and community material remain labelled as leads until reviewed against Cookie Run Classic.

## Deploy

The MVP deploys as a static Vite site through GitHub Pages. Pushes to `main` run `.github/workflows/deploy-pages.yml`, build with `BASE_PATH=/ClassicRunDex/`, and publish `dist/`.

## Launch Loop

Week one runs manually from 2026-07-05 through 2026-07-11. Track daily signals in `docs/week-one-launch-loop.md` and GitHub Issues; do not add analytics, account tracking, coupon collection, invite handling, affiliate links, paid boost flows, or monetized redirects.
